query LoadMenu {
    menu { ...MenuBody,
        children { ...MenuBody,
            children { ...MenuBody,
                children { ...MenuBody }
            }
        }
    }
}

fragment MenuBody on Menu {
    id,
    urn,
    type,
    title,
    orderId
}
