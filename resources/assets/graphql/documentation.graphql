query LoadDocumentation($path: String) {
    docs(path: $path) {
        id
        urn
        url
        title
        content
        isFallback
        updatedAt(format: RFC3339)
        updatedAtHuman: updatedAt(format: HUMAN_READABLE)
        nav {
            slug
            level
            title
        }
    }
}
