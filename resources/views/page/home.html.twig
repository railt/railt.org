{% extends "base.html.twig" %}

{% block title %}Home{% endblock %}

{% block content %}
    {% include "partials/header.html.twig" %}

    <section class="splash container-full" data-vm="SplashViewModel">
        <section class="container">
            <div class="pc"></div>
            <h1 data-bind="style: { top: `${firstLayerY()}px` }, html: title">Railt</h1>
            <h2 data-bind="style: { top: `${firstLayerY()}px` }, css: { ready: ready }">GraphQL Framework</h2>
            <code class="composer" data-bind="style: { top: `${secondLayerY()}px` }">
                <span></span>
                $ composer require railt/railt
            </code>
        </section>
    </section>
{% endblock %}
